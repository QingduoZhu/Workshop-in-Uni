#include <iostream>
int array_sum(int array[],int n)
{
    int sum = 0;
    if (n<1)
    {
        return 0;
    }
    else
    {
        for (i = 0; i<n;i++)
        {
            sum = sum + array[a];
        }
        return sum;
    }
}